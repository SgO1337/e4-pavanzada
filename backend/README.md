# Backend (Spring Boot)

Para ejecutarlo:

- Es necesario tener Java 17+ y Maven instalados.
- Desde la carpeta `backend` ejecutar:

```bash
mvn spring-boot:run
```

Esto incia la aplicación en el puerto 8080: `http://localhost:8080/`.
