# Frontend (Next.js + pnpm)

Para ejecutarlo:

- Instalar pnpm (https://pnpm.io/installation)
- Desde la carpeta `frontend` ejecutar:

```bash
pnpm install
pnpm dev
```

Esto inicia la aplicación en el puerto 3000: `http://localhost:3000/`.
